#!/usr/bin/python3
def pow(a, b):
    """
    Computes 'a' to the power of 'b' (a^b) and returns the value.
    Args:
        a: The base number (integer or float).
        b: The exponent (integer, can be negative or zero).
    Returns:
        The result of a^b.
    """
    return a ** b
